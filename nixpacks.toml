# [phases.build]
# cmds = ["...","php artisan optimize:clear","php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force"]

[start]
cmd = "./start.sh"